# Skills

Professional-level FFmpeg skills are intentionally scheduled for **Milestone 10**.

This directory exists in Milestone 1 so the plugin/package topology is stable from the beginning. No placeholder `SKILL.md` is installed because an incomplete skill could be discovered and invoked by ChatGPT or Codex before its workflow is production-ready.
